# Secret references only — never put plaintext tokens in this file.
# Field is `credential` (API_CREDENTIAL), not the wrangler plugin's `Token`.
CLOUDFLARE_API_TOKEN="op://Developer Vault/Cloudflare Workers API/credential"
